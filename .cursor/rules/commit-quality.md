Whenever the user asks for a commit or uses the command @commit:

- Read all diffs.
- Understand exactly what changed.
- Write a clear, conventional commit message.
- Include feature/workflow context.
- Mention required docs or translation updates if relevant.
- No generic commit messages.
- Output ONLY the commit message.
- Never add explanations or commentary.
